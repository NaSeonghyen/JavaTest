package inheritance_test;

public class Inheritance_ex1 {
	/*
	 * 두개의 클래스를 서로 부모와 자식관계를 맺어준다.(코드의 재사용)
	 * class 자식클래스이름 extends 부모클래스이름
	 * ex( class Child extends Parents )
	 * 
	 * class Parent {}
	 * class Child extends Parent{
	 * }
	 * 자손 클래스는 조상의 모든 클래스를 상속받는다.(생성자는 상속받지 않는다.)
	 * 자손 멤버의 개수는 조상 멤버의 개수보다 적을 수 없다.(같거나 많다.)
	 * 
	 * class Parent{
	 * int age;
	 * }
	 * 
	 * class Child extends Parents {}
	 * 
	 * 자바는 단일 상속만 허용된다. 두개의 조상을 모실수가 없음. C++은 다중상속이 허용된다.
	 * 두개를 상속 받았을때는 같은 메서드 이름이 존재한다만 무엇을 상속받을 지 모르기 때문에
	 * 따라서 비용이 높은 클래스 하나는 상속 나머지 하나는 포함 관계로 둔다.
	 */
	
	

}
